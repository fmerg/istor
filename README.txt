istor
=====

`istor` is a utility for checking if an IP belongs to a 
Tor exit node. Comparison is made for speed against the 
local database of Tor exit nodes established with the
`tornodes` script inside the present repository.
